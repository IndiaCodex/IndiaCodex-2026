{
  "name": "@sentinel/export-json",
  "version": "0.1.0",
  "private": true,
  "description": "ExportPort adapter rendering an ExecutionAuditExport bundle to portable JSON (ADR-0004).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sentinel/domain": "workspace:*"
  },
  "devDependencies": {
    "@sentinel/explainability": "workspace:*",
    "@sentinel/testkit": "workspace:*",
    "@types/node": "^22.19.21",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
