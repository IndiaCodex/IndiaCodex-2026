{
  "name": "@sentinel/adapter-masumi",
  "version": "0.1.0",
  "private": true,
  "description": "MasumiAdapterPort implementation. A clean, replaceable abstraction over Masumi's Payment Service today; the same seam Registry, Runtime, and future Masumi SDKs plug into later — this package, not Sentinel's domain or application layer, is what changes when Masumi's real APIs arrive.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sentinel/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
