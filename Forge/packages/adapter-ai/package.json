{
  "name": "@forge/adapter-ai",
  "version": "0.0.0",
  "description": "Forge platform — ILanguageModelPort implementation. Per ADR-003, its role is strictly limited to intent classification, parameter extraction, and narration of already-computed facts; it never generates blockchain logic. Local, deterministic, and dependency-free by design — no hosted API required for a reliable demo.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@forge/application": "workspace:*",
    "@forge/domain": "workspace:*",
    "@forge/plugin-api": "workspace:*"
  }
}
